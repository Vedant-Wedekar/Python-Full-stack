--------------------------------------------virtual env for django using uv package manager----------------------------------------
🧩 Step 1: Install uv
pip install uv

🧩 Step 2: Add the folder to PATH
Press Windows + R, type:
sysdm.cpl

In the System Properties window, go to:
Advanced → Environment Variables

Under User variables for ASUS, find the variable named Path, then click Edit.

Click New, and paste this exact line:

C:\Users\ASUS\AppData\Roaming\Python\Python313\Scripts

Click OK → OK → OK to save


⚙️ Step 3: (If it still doesn’t show)
uv --version

⚙️ Step 4:
cd "C:\Users\ASUS\OneDrive\Desktop\MCA\Python-Full-stack\my_django_app"

# Create virtual environment
uv venv

# Activate it
.venv\Scripts\activate

# Install Django
uv pip install django

# Create your project
django-admin startproject core .

# Run server
uv run python manage.py runserver


------------------------------------------------------------FOR CREATING APP------------------------------------------------------------------- 

# Activate it
.venv\Scripts\activate

# App command 
python manage.py startapp myapp





----------------------------------------------WANT TO INSTALL TAILWIND----------------------------------------------------------------------- 
(always u have to activate the virtual env first )

python -m ensurepip --upgrade


pip install 'django-tailwind [reload]'      
            or
uv add django django-tailwind


Open settings.py and add:

INSTALLED_APPS = [
    # your other apps
    "tailwind",
]

TAILWIND_APP_NAME = "theme"
INTERNAL_IPS = [
    "127.0.0.1",
]
NPM_BIN_PATH = "C:\\Program Files\\nodejs\\npm.cmd"



python manage.py tailwind init
python manage.py tailwind install
[2/2] Include daisyUI plugin?
1 - no
2 - yes
Choose from [1/2] (1):

python manage.py tailwind start


---------------------------------------------You installed django-browser-reload---------------------------------------------------------

1️⃣ Go to your project folder:

cd "C:\Users\ASUS\OneDrive\Desktop\MCA\Python-Full-stack\my_django_app"


2️⃣ Activate the virtual environment:

.\.venv\Scripts\activate


✅ After this, your terminal prompt should start with:

(my_django_app) PS C:\Users\ASUS\OneDrive\Desktop\MCA\Python-Full-stack\my_django_app>


3️⃣ Install django-browser-reload inside the venv:

python -m pip install django-browser-reload --force-reinstall


4️⃣ Confirm it’s installed in your venv:

pip show django-browser-reload


Make sure the Location is something like:

C:\Users\ASUS\OneDrive\Desktop\MCA\Python-Full-stack\my_django_app\.venv\Lib\site-packages


5️⃣ Now run:

python manage.py runserver


If that still doesn’t work (it should), share the output of:

where python
pip show django-browser-reload









------------------------------------🧩Create and access the Django admin (step-bystep)------------------------------------------------------

1------------Open terminal 
python manage.py migrate

2------------Create the superuser
python manage.py createsuperuser

Username (leave blank to use 'asus'): tgved
Email address:
Password:
Password (again):

If you see messages like:

This password is too common.
This password is entirely numeric.
Bypass password validation and create user anyway? [y/N]:
Type y to force-create (not recommended for production).
Better: choose a stronger password (mix letters, numbers, symbols) and type n to re-enter.
After success you’ll see:
Superuser created successfully.

3------------Start the development server
python manage.py runserver

By default the server runs at http://127.0.0.1:8000/.

Open the admin site
http://127.0.0.1:8000/admin/


Log in with the username tgved (or whatever you entered) and the password you set.
If you changed database settings or added new apps, run python manage.py makemigrations before migrate.
Don’t bypass password validation on production — it’s insecure.
Want to create a superuser non-interactively? You can use environment variables or a custom script, but the interactive method is easiest during development.
If the admin page shows a 404 or you get a redirect: confirm django.contrib.admin is in INSTALLED_APPS and path('admin/', admin.site.urls) exists in your urls.py.






-------------------------------------Resetting Django Superuser Password (Step-by-Step)-------------------------------------------------------
🔐 If you forget your Django superuser password 😅

1️⃣ Create a new superuser (if needed)
python manage.py createsuperuser          

✅ Superuser created successfully!

2️⃣ Change the password for an existing superuser 🔄

If you remember the username (for example, tgved) but forgot the password, you can reset it using:

python manage.py changepassword tgved

Then Django will ask:
Changing password for user 'tgved'
Password:
Password (again):
Password changed successfully for user 'tgved'
3️⃣ Try logging in again 🚀
Go to 👉 http://127.0.0.1:8000/admin/
and log in with your new password 🧠
-----------------------------------------------------------------------------------------------------------------------------------------------




----------------------------------------🧩 Implementing Models & Connecting to Admin Panel--------------------------------------------------

Let’s create our model/data schema, make migrations, and connect it to the Django admin panel 🚀

1️⃣ Create model inside myapp/models.py 🧱
from django.db import models
from django.utils import timezone

# Create your models here.
class pokevar(models.Model):
    poke_choice = [
        ('Pi', 'Pikachu ⚡'),
        ('Ch', 'Charmander 🔥'),
        ('Sq', 'Squirtle 💧'),
        ('Bu', 'Bulbasaur 🌿'),
    ]

    name = models.CharField(max_length=100)
    image = models.ImageField(upload_to='images/')
    date = models.DateTimeField(default=timezone.now)
    type = models.CharField(max_length=100, choices=poke_choice)

    def __str__(self):
        return self.name


✅ Explanation:

name → stores Pokémon name 📝

image → uploads image to /media/images/ 📸

date → auto-adds timestamp 🕒

type → dropdown with Pokémon types 🎮

2️⃣ Register your model in myapp/admin.py 🧑‍💼
from django.contrib import admin
from .models import pokevar

# Register your models here
admin.site.register(pokevar)


✅ Now your model will appear in the Django admin dashboard 🎨

3️⃣ Add media configuration in settings.py ⚙️
MEDIA_URL = '/media/'
MEDIA_ROOT = os.path.join(BASE_DIR, 'media')


✅ This tells Django where to store and how to access uploaded images 🗂️

4️⃣ Add media URL handling in myapp/urls.py 🌐
from django.conf import settings
from django.conf.urls.static import static
from django.urls import re_path
from django.views.static import serve

urlpatterns = [
    # Use re_path for serving media files
    re_path(r'^media/(?P<path>.*)$', serve, {'document_root': settings.MEDIA_ROOT}),
]

# Add this *outside* urlpatterns
if settings.DEBUG:
    urlpatterns += static(settings.MEDIA_URL, document_root=settings.MEDIA_ROOT)


✅ This makes sure your uploaded images are visible during development 🖼️

5️⃣ Run migrations 🧠
python manage.py makemigrations cdd
python manage.py migrate


✅ Django will create your database tables for the pokevar model 🗃️

6️⃣ Open Admin Panel 🎉
python manage.py runserver


Then visit 👉 http://127.0.0.1:8000/admin/

Log in using your superuser credentials, and you’ll see your PokeVar model! 🐉✨


